package g.gitops.poc.app.service.impl;

import g.gitops.poc.app.service.CarrierBookingAppService;
import g.gitops.poc.domain.so.entity.So;
import org.springframework.stereotype.Service;

@Service
public class CarrierBookingAppServiceImpl implements CarrierBookingAppService {
    @Override
    public So assignContainer(So so) {
        return null;
    }
}
